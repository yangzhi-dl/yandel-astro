---
interface Props {
  title: string;
  description?: string;
  href?: string;
  linkLabel?: string;
}

const { title, description, href, linkLabel } = Astro.props;
---

<div class="mb-6 flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
  <div>
    <h2 class="m-0 text-2xl font-semibold text-white md:text-3xl">{title}</h2>
    {description && <p class="mt-3 max-w-2xl leading-7 text-slate-400">{description}</p>}
  </div>

  {
    href && linkLabel && (
      <a class="text-sm font-medium text-cyan-200 transition-colors hover:text-white" href={href}>
        {linkLabel}
      </a>
    )
  }
</div>
