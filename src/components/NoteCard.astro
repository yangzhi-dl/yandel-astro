---
import type { NoteItem } from "../data/home";

interface Props {
  note: NoteItem;
}

const { note } = Astro.props;
---

<article
  class="glass-panel rounded-lg p-5"
>
  <div class="mb-4 flex flex-wrap items-center gap-2 text-xs text-slate-400">
    <span class="glass-chip rounded-md px-2.5 py-1 text-cyan-200">
      {note.platform}
    </span>
    <span>{note.category}</span>
    <span>{note.status}</span>
  </div>

  <h2 class="m-0 text-xl font-semibold text-white">{note.title}</h2>
  <p class="mt-3 leading-7 text-slate-400">{note.summary}</p>

  {
    note.href ? (
      <a class="mt-5 inline-flex text-sm font-medium text-cyan-200 transition-colors hover:text-white" href={note.href}>
        阅读文章
      </a>
    ) : (
      <span class="mt-5 inline-flex text-sm text-slate-500">内容整理中</span>
    )
  }
</article>
